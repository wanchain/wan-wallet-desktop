import React, { Component } from 'react';
import DApp from '../../components/DApp';

class Dex extends Component {
  render () {
    return (
      <DApp dAppUrl={'https://demodex.wandevs.org/'}/>
    );
  }
}

export default Dex;
